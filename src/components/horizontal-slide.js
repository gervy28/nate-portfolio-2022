const BlogPostSelector = ( selector ) => {

  const horizontalScrollElement = document.querySelectorAll(selector);

  

  horizontalScrollElement.forEach(container => {
    container.addEventListener("mousedown"), (event) => {

    };



  });

}